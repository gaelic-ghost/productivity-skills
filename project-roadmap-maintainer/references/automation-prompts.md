# Compatibility Redirect

`project-roadmap-maintainer` is deprecated and retained for compatibility only.

Use canonical prompts from:

- `../project-docs-maintainer/references/roadmap-automation-prompts.md`

Redirect rule:

- Replace `Use $project-roadmap-maintainer` with `Use $project-docs-maintainer with mode=roadmap_maintenance`.
- Preserve existing placeholders and run-mode semantics (`check-only` / `apply`).
