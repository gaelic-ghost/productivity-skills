interface:
  display_name: "Project Roadmap Maintainer"
  short_description: "Compatibility-only redirect for legacy roadmap-maintainer prompts."
  default_prompt: "If a legacy prompt explicitly targets $project-roadmap-maintainer, redirect it to $project-docs-maintainer with mode=roadmap_maintenance and preserve the same target path and run intent."
